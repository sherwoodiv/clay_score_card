export default defineAppConfig({
  ui: {
    colors: {
      primary: 'green',
      neutral: 'slate'
    },
    container: {
      base: 'w-full max-w-(--ui-container) mx-auto px-0 sm:px-0 lg:px-0'
    }
  }
})
